name: setaplogin
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.2.0
  firebase_auth: ^4.4.0
  firebase_core: ^2.25.4
  firebase_storage: ^11.6.7
  cloud_firestore: ^4.15.7
  cloud_firestore_web: ^3.8.4
  cupertino_icons: ^1.0.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
